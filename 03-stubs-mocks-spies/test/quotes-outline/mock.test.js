const quotesLib = require("../../src/quotes");
const sinon = require("sinon");

describe("quotes", () => {
  describe("steve", () => {
    it("should just call fromPerson with the query 'steve jobs'", () => {
      /*
        This test should verify that `steve` is an alias for fromPerson("steve jobs").
      */
    });
  });
});
